package Dijkstra;
import java.util.HashSet;

public class ASet extends HashSet<VertexInterface> implements ASetInterface {

	@Override
	public boolean contains(VertexInterface y) {
		return super.contains(y);
	}

}
